<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Background circle -->
    <item>
        <shape android:shape="oval">
            <solid android:color="@color/surface_variant" />
            <size
                android:width="@dimen/battery_indicator_size"
                android:height="@dimen/battery_indicator_size" />
        </shape>
    </item>

    <!-- Progress circle -->
    <item>
        <rotate
            android:fromDegrees="-90"
            android:toDegrees="270">
            <shape android:shape="ring"
                android:innerRadiusRatio="3.2"
                android:thicknessRatio="8"
                android:useLevel="true">
                <gradient
                    android:startColor="@color/battery_high"
                    android:centerColor="@color/battery_medium"
                    android:endColor="@color/battery_low"
                    android:type="sweep" />
            </shape>
        </rotate>
    </item>
</layer-list>
